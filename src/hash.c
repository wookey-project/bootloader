#include "hash.h"

# if CONFIG_LOADER_FW_HASH_CHECK

bool check_fw_hash(t_firmware_state *fw)
{
    /* Begin to hash the */
    /*TODO not yet implemented */
    
    fw = fw;
    return true;
}

# endif
